﻿namespace Core.Schemas
{
    public enum SchemaPropertyType
    {
        Decimal,
        Int,
        SingleChoice,
        Double,
        Boolean,
        Percentage,
        String,
        Date,
        Enum,
        MatchEnum,
        Id
    }
}
